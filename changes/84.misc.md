Added a live end-to-end test that builds a real wheel for iOS and Android using `xbuild`, exercising the full build pipeline (including real compilation of a C extension) against a minimal sample project.
